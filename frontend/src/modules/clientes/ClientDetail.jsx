/** Ficha operativa del cliente. Equipos aparece solo si el módulo opcional está habilitado. */
import React, { useEffect, useState } from "react";
import axios from "axios";
import { Activity, BarChart3, FileText, Mail, ReceiptText, Ticket, UserRound, Wifi, X, AlertCircle, CheckCircle2, Loader, Save, MapPin, PackageCheck } from "lucide-react";
import ClientServiceEditor from "./editor/ClientServiceEditor";
import ClientBilling from "./editor/ClientBilling";
import ClientCommunications from "./editor/ClientCommunications";
import ClientActivityLog from "./ClientActivityLog";
import ClientEquipmentPanel from "./equipos/ClientEquipmentPanel";
import CoordinatesPicker from "../red/components/CoordinatesPicker";

const baseTabs=[{id:"summary",label:"Resumen",icon:UserRound},{id:"service",label:"Servicio",icon:Wifi},{id:"equipment",label:"Equipos",icon:PackageCheck},{id:"billing",label:"Facturación",icon:ReceiptText},{id:"tickets",label:"Tickets",icon:Ticket},{id:"messages",label:"Email y SMS",icon:Mail},{id:"documents",label:"Documentos",icon:FileText},{id:"stats",label:"Estadísticas",icon:BarChart3},{id:"log",label:"Log",icon:Activity}];
const money=value=>`S/. ${Number(value||0).toFixed(2)}`;
const prettyStatus=status=>status==="active"?"Activo":status==="suspended"?"Suspendido":status||"Sin estado";
function EmptyState({title,description}){return <div className="rounded-xl border border-dashed border-slate-700 bg-slate-950/40 p-8 text-center"><p className="font-semibold text-slate-300">{title}</p><p className="mt-1 text-sm text-slate-500">{description}</p></div>;}
function Value({label,children}){return <div className="rounded-xl border border-slate-800 bg-slate-950/50 p-3"><p className="text-[11px] uppercase tracking-wide text-slate-500">{label}</p><p className="mt-1 break-words text-sm font-medium text-slate-200">{children||"Sin registrar"}</p></div>;}
const inputClass="rounded-xl border border-slate-700 bg-slate-950 px-3 py-2 text-sm text-slate-200 placeholder-slate-600 focus:border-cyan-500 focus:outline-none";
const requiredLabel=text=><span>{text} <span className="text-rose-400" aria-hidden="true">*</span></span>;

export default function ClientDetail({clientId,api,token,onClose,onClientUpdated}){
  const [activeTab,setActiveTab]=useState("summary"),[client,setClient]=useState(null),[loading,setLoading]=useState(true),[error,setError]=useState("");
  const [summaryFormData,setSummaryFormData]=useState({full_name:"",dni_ruc:"",phone:"",email:"",address:"",reference:"",installation_date:"",zone_id:"",latitude:"",longitude:""});
  const [zones,setZones]=useState([]),[summarySaving,setSummarySaving]=useState(false),[summaryError,setSummaryError]=useState(""),[summarySuccess,setSummarySuccess]=useState(""),[showLocationPicker,setShowLocationPicker]=useState(false),[equipmentEnabled,setEquipmentEnabled]=useState(false);
  const headers={Authorization:`Bearer ${token}`};
  const tabs=baseTabs.filter(tab=>tab.id!=="equipment"||equipmentEnabled);
  const logActivity=async(action,detail)=>{try{await axios.post(`${api}/clients/${clientId}/activity`,{action,detail},{headers});}catch(err){console.error("No se pudo registrar actividad del cliente:",err);}};
  const reloadClient=async()=>{const response=await axios.get(`${api}/clients/${clientId}`,{headers});setClient(response.data);return response.data;};

  useEffect(()=>{let alive=true;const load=async()=>{setLoading(true);setError("");try{const [clientRes,zonesRes,equipmentRes]=await Promise.all([axios.get(`${api}/clients/${clientId}`,{headers}),axios.get(`${api}/zones`,{headers}),axios.get(`${api}/client-equipment/enabled`,{headers})]);if(!alive)return;const c=clientRes.data;setClient(c);setZones(zonesRes.data||[]);setEquipmentEnabled(equipmentRes.data?.enabled===true);setSummaryFormData({full_name:c.full_name||"",dni_ruc:c.dni_ruc||"",phone:c.phone||"",email:c.email||"",address:c.address||"",reference:c.reference||"",installation_date:c.installation_date||"",zone_id:c.zone_id||"",latitude:c.latitude!=null?String(c.latitude):"",longitude:c.longitude!=null?String(c.longitude):""});}catch(err){if(alive)setError(err.response?.data?.detail||"No se pudo cargar la ficha del cliente.");}finally{if(alive)setLoading(false);}};load();return()=>{alive=false;};},[api,clientId,token]);
  const handleSummaryFormChange=e=>{const{name,value}=e.target;setSummaryFormData(prev=>({...prev,[name]:value}));};
  const openGoogleMaps=()=>{setSummaryError("");setShowLocationPicker(true);};
  const applyLocation=({lat,lng})=>{setSummaryFormData(prev=>({...prev,latitude:lat.toFixed(6),longitude:lng.toFixed(6)}));setShowLocationPicker(false);};
  const handleSaveSummary=async e=>{e.preventDefault();setSummarySaving(true);setSummaryError("");setSummarySuccess("");try{const fullName=summaryFormData.full_name.trim(),dniRuc=summaryFormData.dni_ruc.trim(),phone=summaryFormData.phone.trim();if(!fullName)throw new Error("El nombre del cliente es obligatorio.");if(!dniRuc)throw new Error("El DNI/RUC es obligatorio.");if(!phone)throw new Error("El Celular / WhatsApp es obligatorio.");if(summaryFormData.latitude||summaryFormData.longitude){const lat=parseFloat(summaryFormData.latitude),lng=parseFloat(summaryFormData.longitude);if(Number.isNaN(lat)||Number.isNaN(lng))throw new Error("Latitud y longitud deben ser números válidos.");if(lat< -90||lat>90||lng< -180||lng>180)throw new Error("Coordenadas fuera de rango válido.");}const payload={full_name:fullName,dni_ruc:dniRuc,phone,email:summaryFormData.email.trim(),address:summaryFormData.address.trim(),reference:summaryFormData.reference.trim(),installation_date:summaryFormData.installation_date||null,zone_id:summaryFormData.zone_id||null,latitude:summaryFormData.latitude?parseFloat(summaryFormData.latitude):null,longitude:summaryFormData.longitude?parseFloat(summaryFormData.longitude):null};await axios.patch(`${api}/clients/${clientId}/summary`,payload,{headers});await logActivity("Datos personales editados","Se actualizaron los datos de Resumen del cliente.");await reloadClient();setSummarySuccess("Datos del cliente actualizados correctamente.");onClientUpdated?.();}catch(err){setSummaryError(err.response?.data?.detail||err.message||"Error al guardar los cambios.");}finally{setSummarySaving(false);}};
  const handleServiceSaveSuccess=async(event={})=>{try{await reloadClient();if(event.type==="delete"){onClientUpdated?.();return;}const service=event.service||{},action=event.type==="create"?"Servicio creado":"Servicio editado",detail=[`${event.type==="create"?"Se creó":"Se actualizó"} el servicio${service.plan_name?` del plan ${service.plan_name}`:""}.`,`Precio: ${money(service.plan_price)}.`,`Conexión: ${service.connection_type||"sin especificar"}.`,`Tecnología: ${service.technology==="wireless"?"Inalámbrico":service.technology==="fiber"?"Fibra óptica":"sin especificar"}.`,`IP: ${service.ip_address||"sin IP"}.`,`Usuario PPPoE: ${service.pppoe_user||"sin usuario"}.`,`MikroTik: ${service.router_name||service.router_id||"sin asignar"}.`,`Zona: ${service.zone_name||service.zone_id||"sin zona"}.`].join(" ");await logActivity(action,detail);onClientUpdated?.();}catch(err){console.error("Error recargando cliente:",err);}};
  const handleBalanceUpdate=async()=>{try{await reloadClient();await logActivity("Facturación actualizada","Se realizó una acción en Facturación del cliente: factura, pago, anulación, eliminación o saldo.");}catch(err){console.error("Error recargando cliente:",err);}};

  const content=()=>{if(loading)return <div className="py-16 text-center text-slate-400">Cargando información del cliente…</div>;if(error)return <div className="py-16 text-center text-rose-400">{error}</div>;if(!client)return null;if(activeTab==="summary")return <div className="grid gap-5 lg:grid-cols-3"><section className="client-detail-summary space-y-5 lg:col-span-2">{summaryError&&<div className="flex gap-3 rounded-xl border border-rose-500/30 bg-rose-500/10 p-4"><AlertCircle className="h-5 w-5 shrink-0 text-rose-300"/><p className="text-sm text-rose-300">{summaryError}</p></div>}{summarySuccess&&<div className="flex gap-3 rounded-xl border border-emerald-500/30 bg-emerald-500/10 p-4"><CheckCircle2 className="h-5 w-5 shrink-0 text-emerald-300"/><p className="text-sm text-emerald-300">{summarySuccess}</p></div>}<form onSubmit={handleSaveSummary} className="space-y-5"><div><h3 className="mb-3 text-base font-bold text-white">Datos de identidad</h3><div className="grid gap-3 sm:grid-cols-2"><label className="space-y-1 text-xs text-slate-400">{requiredLabel("Nombre completo o razón social")}<input type="text" name="full_name" value={summaryFormData.full_name} onChange={handleSummaryFormChange} required className={`w-full ${inputClass}`}/></label><label className="space-y-1 text-xs text-slate-400">{requiredLabel("DNI / RUC")}<input type="text" name="dni_ruc" value={summaryFormData.dni_ruc} onChange={handleSummaryFormChange} required className={`w-full ${inputClass}`}/></label></div></div><div><h3 className="mb-3 text-base font-bold text-white">Contacto</h3><div className="grid gap-3 sm:grid-cols-2"><label className="space-y-1 text-xs text-slate-400">{requiredLabel("Celular / WhatsApp")}<input type="tel" name="phone" value={summaryFormData.phone} onChange={handleSummaryFormChange} required className={`w-full ${inputClass}`}/></label><label className="space-y-1 text-xs text-slate-400">Correo electrónico<input type="email" name="email" value={summaryFormData.email} onChange={handleSummaryFormChange} className={`w-full ${inputClass}`}/></label></div></div><div><div className="mb-3 flex flex-wrap items-center justify-between gap-2"><h3 className="text-base font-bold text-white">Ubicación</h3><button type="button" onClick={openGoogleMaps} className="inline-flex items-center gap-2 rounded-lg border border-cyan-500/40 bg-cyan-500/10 px-3 py-2 text-xs font-semibold text-cyan-300"><MapPin className="h-4 w-4"/>Seleccionar ubicación</button></div><div className="space-y-3"><input type="text" name="address" value={summaryFormData.address} onChange={handleSummaryFormChange} placeholder="Dirección de instalación" className={`w-full ${inputClass}`}/><input type="text" name="reference" value={summaryFormData.reference} onChange={handleSummaryFormChange} placeholder="Referencia" className={`w-full ${inputClass}`}/><select name="zone_id" value={summaryFormData.zone_id} onChange={handleSummaryFormChange} className={`w-full ${inputClass}`}><option value="">-- Selecciona una zona --</option>{zones.map(zone=><option key={zone.id} value={zone.id}>{zone.name}</option>)}</select></div></div><div><h3 className="mb-3 text-base font-bold text-white">Coordenadas GPS</h3><div className="grid gap-3 sm:grid-cols-2"><input type="number" name="latitude" value={summaryFormData.latitude} onChange={handleSummaryFormChange} step="0.000001" min="-90" max="90" className={inputClass}/><input type="number" name="longitude" value={summaryFormData.longitude} onChange={handleSummaryFormChange} step="0.000001" min="-180" max="180" className={inputClass}/></div></div><div><h3 className="mb-3 text-base font-bold text-white">Instalación</h3><input type="date" name="installation_date" value={summaryFormData.installation_date} onChange={handleSummaryFormChange} className={inputClass}/></div><div className="flex justify-end pt-4"><button type="submit" disabled={summarySaving} className="flex items-center gap-2 rounded-lg bg-cyan-600 px-6 py-2 text-sm font-medium text-white disabled:opacity-50">{summarySaving?<><Loader className="h-4 w-4 animate-spin"/>Guardando…</>:<><Save className="h-4 w-4"/>Guardar cambios</>}</button></div></form></section><section className="client-detail-account rounded-2xl border border-slate-800 bg-slate-950/55 p-5"><h3 className="text-base font-bold text-white">Estado de cuenta</h3><div className="mt-4 space-y-3"><div className={`rounded-xl px-3 py-2 text-sm font-semibold ${client.status==="active"?"bg-emerald-500/15 text-emerald-300":"bg-rose-500/15 text-rose-300"}`}>Servicio {prettyStatus(client.status)}</div><Value label="Plan contratado">{client.plan_name}</Value><Value label="Pago mensual">{money(client.plan_price)}</Value><Value label="Deuda actual">{money(client.balance_due)}</Value><Value label="Facturas pendientes">{client.unpaid_invoices_count||0}</Value><Value label="Día de pago">Día {client.billing_day||"Sin registrar"}</Value></div></section></div>;
    if(activeTab==="service")return <ClientServiceEditor clientId={clientId} api={api} token={token} onSaveSuccess={handleServiceSaveSuccess}/>;
    if(activeTab==="equipment"&&equipmentEnabled)return <ClientEquipmentPanel clientId={clientId} api={api} token={token}/>;
    if(activeTab==="billing")return <ClientBilling clientId={clientId} api={api} token={token} onBalanceUpdate={handleBalanceUpdate}/>;
    if(activeTab==="messages")return <ClientCommunications clientId={clientId} api={api} token={token}/>;
    if(activeTab==="tickets")return <EmptyState title="Tickets" description="Módulo de tickets disponible desde la gestión general del cliente."/>;
    if(activeTab==="documents")return <EmptyState title="Documentos" description="El módulo de documentos se encuentra disponible para integrar en la ficha."/>;
    if(activeTab==="stats")return <EmptyState title="Estadísticas" description="Las estadísticas del cliente se mostrarán aquí."/>;
    if(activeTab==="log")return <ClientActivityLog activities={client.activities||[]}/>;return null;};
  if(showLocationPicker)return <CoordinatesPicker title={`Ubicación · ${client?.full_name||"Cliente"}`} latitude={summaryFormData.latitude} longitude={summaryFormData.longitude} onApply={applyLocation} onClose={()=>setShowLocationPicker(false)}/>;
  return <div className="fixed inset-0 z-[60] flex items-center justify-center bg-slate-950/85 p-3 backdrop-blur-sm sm:p-6" onMouseDown={e=>{if(e.target===e.currentTarget&&!summarySaving)onClose?.();}}><div className="client-detail-modal flex max-h-[94vh] w-full max-w-6xl flex-col overflow-hidden rounded-2xl border border-slate-700 bg-slate-900 shadow-2xl" onMouseDown={e=>e.stopPropagation()}><header className="flex shrink-0 items-center justify-between border-b border-slate-800 px-5 py-4"><div><h2 className="text-lg font-bold text-white">{client?.full_name||"Cliente"}</h2><p className="text-xs text-slate-500">Ficha del cliente</p></div><button type="button" onClick={onClose} className="rounded-lg p-2 text-slate-400 hover:bg-slate-800 hover:text-white"><X className="h-5 w-5"/></button></header><nav className="flex shrink-0 gap-1 overflow-x-auto border-b border-slate-800 px-4 py-2">{tabs.map(tab=>{const Icon=tab.icon;return <button key={tab.id} type="button" onClick={()=>setActiveTab(tab.id)} className={`flex shrink-0 items-center gap-2 rounded-lg px-3 py-2 text-xs font-semibold transition ${activeTab===tab.id?"bg-cyan-500/15 text-cyan-300":"text-slate-400 hover:bg-slate-800 hover:text-slate-200"}`}><Icon className="h-4 w-4"/>{tab.label}</button>;})}</nav><main className="min-h-0 flex-1 overflow-y-auto p-5">{content()}</main></div></div>;
}
