# Backup de integración AutomatizadoVIP

Los componentes nuevos de la integración se mantienen aislados. Los archivos existentes no se sustituyen en esta fase salvo los puntos mínimos de registro necesarios para montar los routers/componentes.

La credencial real de AutomatizadoVIP no se guarda en GitHub.

Estado previo protegido: mensajería existente con `wa.me`, plantillas de `/api/messaging/templates` y configuración general de `Setting`.
